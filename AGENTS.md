# 打包交付规则

- 后续打包交付不再执行首次启动、隔离环境启动、Flet 会话或首页加载验证，也不自动启动成品程序进行验收。
- 不把首次启动验证列入计划、必做检查或交付前置条件，不反复询问是否需要执行。
- 保留与本次改动相关的静态检查、包内源码一致性、包内容检查和 ZIP 完整性校验。
- 验证报告如保留启动检查字段，标记为“按用户要求跳过”，不得标记为通过。
- 只有用户后续明确要求启动验证时，才针对该次任务执行。
